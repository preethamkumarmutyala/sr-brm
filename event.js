const p=new URLSearchParams(window.location.search);const id=p.get("id");fetch("data/events.json").then(r=>r.json()).then(events=>{const e=events.find(x=>x.id===id);document.getElementById("eventContainer").innerHTML=`<div class="section"><h1>${e.name}</h1><p><b>Event Date:</b> ${e.date}</p><p><b>Registration Closes:</b> ${e.registrationClose}</p><p><b>Start:</b> ${e.start}</p><p><b>Registered Riders:</b> ${e.registered}</p><p><a class="button" target="_blank" href="${e.route}">Route Map</a></p></div><div class="section"><h2>Controls</h2>${e.controls.map(c=>`<div class="control">${c.km} km - ${c.location}</div>`).join('')}</div>`})